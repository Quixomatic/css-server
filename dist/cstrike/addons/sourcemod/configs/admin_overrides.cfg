/**
 * Admin Command Overrides
 *
 * Override the default permission requirements for specific commands.
 * Useful for giving certain commands to lower-level admins.
 */
"Overrides"
{
    /**
     * Command overrides
     * Format: "command" "flag"
     */

    // Example: Allow anyone with kick permission to change maps
    // "sm_map"        "c"

    // Example: Restrict !rcon to root admins only
    // "sm_rcon"       "z"
}

/**
 * Group overrides
 * Format: "group" "flag"
 */
"OverrideGroups"
{
    // Example: Give "Moderators" group access to reserved slots
    // "Moderators"    "a"
}
