# ============================================
# COUNTER-STRIKE: SOURCE SERVER CONFIGURATION
# ============================================
# Copy this file to .env and customize your settings.
# All environment variables are optional - defaults are shown.
#
# In Dockge: These will appear as editable fields in the UI.

# ============ SERVER IDENTITY ============
# Server name shown in the server browser
CSS_HOSTNAME=Counter-Strike Source Server

# Password to join the server (leave empty for public)
CSS_PASSWORD=

# RCON password for remote administration (CHANGE THIS!)
RCON_PASSWORD=

# Admin contact email (shown in server info)
CSS_CONTACT=

# Server region (255 = worldwide)
# 0=US East, 1=US West, 2=South America, 3=Europe, 4=Asia, 5=Australia, 6=Middle East, 7=Africa
CSS_REGION=255

# ============ NETWORK ============
# Server port (default: 27015)
CSS_PORT=27015

# Maximum players (including spectators)
CSS_MAXPLAYERS=24

# Server tickrate (66 or 100 recommended)
CSS_TICKRATE=66

# Bandwidth limits (0 = unlimited)
CSS_MAXRATE=0
CSS_MINRATE=20000
CSS_MAXUPDATERATE=100
CSS_MINUPDATERATE=30

# ============ GAMEPLAY ============
# Starting map
CSS_MAP=de_dust2

# Friendly fire (0=off, 1=on)
CSS_FRIENDLYFIRE=0

# All talk - can dead/opposite team hear voice (0=off, 1=on)
CSS_ALLTALK=0

# Map time limit in minutes (0=unlimited)
CSS_TIMELIMIT=30

# Round time in minutes
CSS_ROUNDTIME=2.5

# Freeze time before round starts (seconds)
CSS_FREEZETIME=6

# Buy time in seconds
CSS_BUYTIME=90

# Starting money
CSS_STARTMONEY=800

# C4 bomb timer in seconds
CSS_C4TIMER=45

# Max rounds per map (0=unlimited)
CSS_MAXROUNDS=0

# Win limit to end map (0=unlimited)
CSS_WINLIMIT=0

# Gravity (800=normal, lower=moon gravity)
CSS_GRAVITY=800

# Fall damage (0=off, 1=on)
CSS_FALLDAMAGE=1

# ============ BOTS ============
# Number of bots (0=none)
# With "fill" mode, this is the total player target (bots + humans)
CSS_BOT_QUOTA=0

# Bot quota mode:
#   fill   - Fill to quota (bots leave when humans join)
#   match  - Match human count
#   normal - Fixed bot count
CSS_BOT_QUOTA_MODE=fill

# Bot difficulty (0=easy, 1=normal, 2=hard, 3=expert)
CSS_BOT_DIFFICULTY=2

# Bot chatter (off, minimal, normal, radio)
CSS_BOT_CHATTER=minimal

# Bots wait for human players before joining (0=no, 1=yes)
CSS_BOT_JOIN_AFTER_PLAYER=1

# Bots leave to make room for humans (0=no, 1=yes)
CSS_BOT_AUTO_VACATE=1

# Bot name prefix
CSS_BOT_PREFIX=[BOT]

# ============ SECURITY ============
# sv_pure level for file consistency checking
#   0 = Disabled (allow custom content)
#   1 = Loose (some custom content allowed)
#   2 = Strict (no custom content - recommended for competitive)
CSS_PURE=2

# Allow cheat commands (0=no, 1=yes) - KEEP THIS OFF
CSS_CHEATS=0

# Allow clients to upload custom content (0=no, 1=yes)
CSS_ALLOWUPLOAD=0

# Allow clients to download maps/sounds (0=no, 1=yes)
CSS_ALLOWDOWNLOAD=1

# RCON ban penalty in seconds after failed attempts
CSS_RCON_BANPENALTY=30

# RCON max failures before ban
CSS_RCON_MAXFAILURES=10

# ============ FAST DOWNLOAD ============
# URL for fast HTTP downloads (leave empty if not using)
# Example: http://content.example.com/css/
CSS_DOWNLOADURL=

# Max file size for downloads in MB
CSS_MAXFILESIZE=64

# ============ LOGGING ============
# Enable logging (on/off)
CSS_LOG=on

# Log bans (0=no, 1=yes)
CSS_LOGBANS=1

# Log detail level (0=none, 1=basic, 2=more, 3=full)
CSS_LOGDETAIL=3

# ============ STEAM AUTHENTICATION ============
# Game Server Login Token - REQUIRED for public (non-LAN) servers!
# Without this, your server will only be visible on LAN.
#
# Get your token at: https://steamcommunity.com/dev/managegameservers
# App ID for CS:S is 240 (use 240, not 232330)
#
STEAM_GSLT=
